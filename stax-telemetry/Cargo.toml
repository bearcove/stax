[package]
name = "stax-telemetry"
version = "0.1.0"
edition = "2024"
rust-version = "1.91"
license = "MIT OR Apache-2.0"
description = "Bounded, low-overhead telemetry primitives for stax diagnostics."

[dependencies]
facet = { version = "0.46" }
